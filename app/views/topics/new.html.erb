<%= form_for @topic do |f| %>
<div class="form-group col-md-4 media">
  <%= f.label :title %>
  <%= f.text_field :title, class: 'form-control', placeholder: "Enter topic title" %>
  <%= f.submit "Save", class: 'btn btn-success media' %>
  <% end %>
